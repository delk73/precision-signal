# replay-core

STATUS: experimental

Purpose: shared low-level replay artifact definitions and encoding helpers for exploratory replay work.

Non-goals: stable public API guarantees, operator-facing tooling, or release-qualified workflow ownership.

This crate is not part of the release surface and is not required for `make gate`.

Exit criteria: promote only after its wire/API contract, tests, and downstream usage are explicitly scoped into the release surface and release verification.
