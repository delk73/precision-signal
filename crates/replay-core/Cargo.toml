[package]
name = "replay-core"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Experimental replay core scaffolding outside the v1 release surface"

[features]
default = []

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(kani)'] }
