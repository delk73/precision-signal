[package]
name = "replay-cli"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Experimental replay CLI scaffolding (not part of the v1 release surface)"

[dependencies]
replay-host = { path = "../replay-host" }
